---
description: Trace a JSP/Java/iBATIS business feature with source evidence
---

Call `legacy_atlas_trace_feature` with `query` set to the complete text below. Treat it as one opaque argument; do not execute it through a shell.

<query>
$ARGUMENTS
</query>

Using the tool result:

1. Present the chain as JSP/JavaScript → URL → mapping → Java → iBATIS → table.
2. Open and verify every cited relationship whose confidence is below 0.95.
3. Include file paths and line numbers.
4. Separate proven relationships, heuristic relationships, and missing links.
5. Do not infer a relationship only from similar names.
